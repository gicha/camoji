name: camoji
description: A new Flutter project.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  dio:
  cached_network_image:
  photo_view:
  path_provider:
  url_launcher:
  sembast:
  location:
  sliding_up_panel:
  shimmer:
  http:
  encrypt:
  crypto:
  flutter_spinkit:
  flutter_svg:
  permission_handler:
  auto_size_text:
  provider:
  stacked:
  i18n_extension:
  cupertino_back_gesture:
  eva_icons_flutter:
  page_transition:
  google_fonts:
  flutter_staggered_animations:
  flare_flutter:
  roslib:


dev_dependencies:
  build:
  build_config:
  built_value_generator:
  json_serializable:
  build_runner:


flutter:
  uses-material-design: true
  assets:
     # Flare animations
    - assets/flare/
    # Images
    
    # Icons

  fonts:
    - family: SFProRounded
      fonts:
        - asset: assets/fonts/100.otf
          weight: 100
        - asset: assets/fonts/200.otf
          weight: 200
        - asset: assets/fonts/300.otf
          weight: 300
        - asset: assets/fonts/400.otf
          weight: 400
        - asset: assets/fonts/500.otf
          weight: 500
        - asset: assets/fonts/600.otf
          weight: 600
        - asset: assets/fonts/700.otf
          weight: 700
        - asset: assets/fonts/800.otf
          weight: 800
        - asset: assets/fonts/900.otf
          weight: 900
    